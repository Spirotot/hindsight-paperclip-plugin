{
  "name": "@hindsight-integrations/paperclip",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Auto-recall on heartbeat start, auto-retain on heartbeat completion",
  "author": "InfraFixer",
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ./vitest.config.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@paperclipai/plugin-sdk": "^2026.416.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
